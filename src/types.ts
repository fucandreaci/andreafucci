export type Site = {
  NAME: string;
  EMAIL: string;
  GITHUB: string;
  LINKEDIN: string;
  INSTAGRAM: string;
};

export type Metadata = {
  TITLE: string;
  DESCRIPTION: string;
};

export type NavLink = {
  label: string;
  href: string;
};
